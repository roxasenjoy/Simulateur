{% extends 'base.html.twig' %}

{% block title %}Credits{% endblock %}

{% block body %}
    <h1>Credits</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ credit.id }}</td>
            </tr>
            <tr>
                <th>creditMontant</th>
                <td>{{ credit.creditMontant }}</td>
            </tr>
            <tr>
                <th>creditDuree</th>
                <td>{{ credit.creditDuree }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('credits_index') }}">back to list</a>

    <a href="{{ path('credits_edit', {'id': credit.id}) }}">edit</a>

    {{ include('credits/_delete_form.html.twig') }}
{% endblock %}
