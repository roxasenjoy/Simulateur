<!-- Gestion des messages d'erreurs lors de la validation du formulaire -->
    <div>
        {% if errors|length > 0 %}
            {# ... display the global form errors #}
            <ul>
                {% for error in errors %}
                    <li class="errors">{{ error.message }}</li>
                {% endfor %}
            </ul>

        {% endif %}
    </div>
