{% extends 'base.html.twig' %}

{% block title %}Epargnes{% endblock %}

{% block body %}
    <h1>Epargnes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ epargne.id }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ epargne.montant }}</td>
            </tr>
            <tr>
                <th>Apport</th>
                <td>{{ epargne.apport }}</td>
            </tr>
            <tr>
                <th>reductionImpot</th>
                <td>{{ epargne.reductionImpot }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('epargnes_index') }}">back to list</a>

    <a href="{{ path('epargnes_edit', {'id': epargne.id}) }}">edit</a>

    {{ include('epargnes/_delete_form.html.twig') }}
{% endblock %}
