{% extends 'base.html.twig' %}

{% block title %}Situations{% endblock %}

{% block body %}
  <  <h1>Situations Show</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ situation.id }}</td>
            </tr>
            <tr>
                <th>Famille</th>
                <td>{{ situation.famille }}</td>
            </tr>
            <tr>
                <th>Naissance</th>
                <td>{{ situation.naissance }}</td>
            </tr>
            <tr>
                <th>Enfant</th>
                <td>{{ situation.enfant }}</td>
            </tr>
            <tr>
                <th>enfantFiscal</th>
                <td>{{ situation.enfantFiscal }}</td>
            </tr>
            <tr>
                <th>Pension</th>
                <td>{{ situation.pension }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('situations_index') }}">back to list</a>

    <a href="{{ path('situations_edit', {'id': situation.id}) }}">edit</a>

    {{ include('situations/_delete_form.html.twig') }}
{% endblock %}
